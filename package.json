{
  "name": "arisris",
  "private": true,
  "version": "1.0.0",
  "description": "Arisris Monorepo Project",
  "type": "module",
  "scripts": {
    "postinstall": "bun run typegen",
    "dev": "bun run -F @arisris/app-* dev",
    "build": "bun run -F @arisris/app-* build",
    "preview": "bun run -F @arisris/app-* preview",
    "clean": "bun run -F @arisris/* clean && rm -rf node_modules && bun i",
    "format": "bun --bun biome format",
    "format:fix": "bun --bun biome format --write",
    "lint": "bun --bun biome lint",
    "lint:fix": "bun --bun biome lint --fix",
    "typegen": "bun run -F '@arisris/*' typegen",
    "pkg:create": "bun scripts/create-package.ts",
    "pkg:set-version": "bun scripts/set-version.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "@types/bun": "^1.2.20",
    "typescript": "^5.9.2"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "hono": "^4.9.2",
      "vite": "^7.1.3",
      "wrangler": "4.32.0"
    },
    "catalogs": {
      "svelte-kit": {
        "@sveltejs/adapter-cloudflare": "^7.2.2",
        "@sveltejs/kit": "^2.36.2",
        "@tailwindcss/vite": "^4.1.12",
        "svelte": "^5.38.3",
        "tailwindcss": "^4.1.12"
      }
    }
  },
  "overrides": {
    "esbuild": "0.25.9",
    "wrangler": "4.32.0"
  },
  "patchedDependencies": {
    "wrangler@4.32.0": "patches/wrangler@4.32.0.patch"
  }
}
